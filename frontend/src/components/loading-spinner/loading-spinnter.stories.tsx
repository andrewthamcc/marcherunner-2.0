import React from 'react'
import { Meta } from '@storybook/react'
import { LoadingSpinner } from './loading-spinner'

export default {
  title: 'Atoms/LoadingSpinner',
  component: LoadingSpinner,
}

export const Simple = () => <LoadingSpinner />
